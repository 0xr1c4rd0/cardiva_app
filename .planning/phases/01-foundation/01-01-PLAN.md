---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - .env.local.example
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/lib/utils.ts
autonomous: true

must_haves:
  truths:
    - "Next.js 16 development server starts without errors"
    - "TypeScript compilation succeeds with strict mode"
    - "Tailwind CSS utility classes render correctly"
    - "Project structure follows App Router conventions"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "next"
    - path: "src/app/layout.tsx"
      provides: "Root layout with metadata"
      exports: ["default"]
    - path: "src/app/page.tsx"
      provides: "Home page placeholder"
      exports: ["default"]
    - path: "src/lib/utils.ts"
      provides: "Utility functions including cn() for classnames"
      exports: ["cn"]
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS import"
      pattern: "import.*globals.css"
---

<objective>
Create the Next.js 16 project scaffold with all required dependencies and folder structure.

Purpose: Establish the foundation for the Cardiva RFP Matching App with modern tooling (TypeScript, Tailwind CSS v4, App Router, Turbopack).

Output: A working Next.js 16 development environment with proper folder structure ready for design system and Supabase integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js 16 project with dependencies</name>
  <files>
    package.json
    tsconfig.json
    next.config.ts
    .env.local.example
  </files>
  <action>
    1. Run `npx create-next-app@latest . --yes` in the project root to scaffold Next.js 16 with recommended defaults (TypeScript, Tailwind CSS v4, ESLint, App Router, Turbopack, @/* import alias)

    2. Install additional dependencies:
       ```bash
       npm install @supabase/supabase-js @supabase/ssr zustand @tanstack/react-query clsx tailwind-merge
       ```

    3. Create `.env.local.example` with placeholder Supabase variables:
       ```
       NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
       NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
       ```

    Note: Do NOT create tailwind.config.js - Tailwind v4 uses CSS-first configuration via @theme directive (handled in Plan 02).
  </action>
  <verify>
    - `npm run dev` starts without errors on http://localhost:3000
    - `npm run build` completes successfully
    - package.json contains all required dependencies
  </verify>
  <done>
    Next.js 16 project created with TypeScript, Tailwind CSS v4, and all dependencies installed. Development server runs successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Set up project folder structure and utilities</name>
  <files>
    src/app/layout.tsx
    src/app/page.tsx
    src/lib/utils.ts
    src/components/.gitkeep
    src/components/ui/.gitkeep
    src/components/layout/.gitkeep
    src/hooks/.gitkeep
    src/types/.gitkeep
  </files>
  <action>
    1. Create folder structure:
       ```
       src/
       ├── app/                      # App Router pages (already exists)
       ├── components/
       │   ├── ui/                   # shadcn/ui components (added in Plan 02)
       │   ├── layout/               # Layout components (sidebar, header)
       │   └── shared/               # Shared custom components
       ├── lib/
       │   ├── supabase/             # Supabase clients (added in Plan 03)
       │   └── utils.ts              # General utilities
       ├── hooks/                    # Custom React hooks
       └── types/                    # TypeScript types
       ```

    2. Create `src/lib/utils.ts` with cn() utility:
       ```typescript
       import { type ClassValue, clsx } from "clsx"
       import { twMerge } from "tailwind-merge"

       export function cn(...inputs: ClassValue[]) {
         return twMerge(clsx(inputs))
       }
       ```

    3. Update `src/app/layout.tsx` to include proper metadata:
       ```typescript
       import type { Metadata } from "next"
       import "./globals.css"

       export const metadata: Metadata = {
         title: "Cardiva - RFP Matching",
         description: "Match government RFP products against inventory",
       }

       export default function RootLayout({
         children,
       }: {
         children: React.ReactNode
       }) {
         return (
           <html lang="en">
             <body className="antialiased">
               {children}
             </body>
           </html>
         )
       }
       ```

    4. Update `src/app/page.tsx` with placeholder:
       ```typescript
       export default function Home() {
         return (
           <main className="flex min-h-screen items-center justify-center">
             <div className="text-center">
               <h1 className="text-4xl font-semibold text-gray-900">Cardiva</h1>
               <p className="mt-2 text-gray-600">RFP Matching Application</p>
             </div>
           </main>
         )
       }
       ```

    5. Add .gitkeep files to empty directories to preserve structure in git.
  </action>
  <verify>
    - All directories exist in src/
    - `src/lib/utils.ts` exports cn function
    - `npm run dev` still works after changes
    - Home page displays "Cardiva" heading at localhost:3000
  </verify>
  <done>
    Project folder structure created following Next.js App Router conventions. Utility functions in place. Placeholder pages render correctly.
  </done>
</task>

</tasks>

<verification>
1. Run `npm run dev` and verify localhost:3000 shows Cardiva placeholder
2. Run `npm run build` and verify no TypeScript or build errors
3. Verify folder structure matches specification
4. Verify all dependencies in package.json
</verification>

<success_criteria>
- Next.js 16 development server runs without errors
- TypeScript compilation passes with strict mode
- Project structure follows App Router conventions
- All required dependencies installed
- Environment variable template exists
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
